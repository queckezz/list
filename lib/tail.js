
var drop = require('./drop')

/**
 * Returns all elements in an `array` except the first.
 *
 * @function tail
 * @param {Array} array Array to operate on
 * @return {Array} All items except the first
 */

module.exports = drop(1)